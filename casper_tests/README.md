Running Phantom Functional Tests
================================

1. Install CasperJS (http://casperjs.org/)
2. set PHANTOM_USERNAME, PHANTOM_PASSWORD, and optionally PHANTOM_URL envs
3. run run-tests:

 $ ./run-tests
